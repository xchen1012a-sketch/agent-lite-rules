---
name: project-planning
description: 规划项目、阶段切换和范围控制。用于项目初始化、架构调整、跨仓协作、规则接入、阶段推进，或任何需要把工作拆成可验收阶段的任务。
---

# Project Planning

## 目标

把不清楚、范围大的工作拆成小的、可验证、可恢复的阶段。计划不是仪式；只有当范围、阶段、架构或多人/多仓协作真的需要它时才维护。

## 适用场景

- 新项目启动、旧项目接入、规则迁移。
- 阶段切换、里程碑验收、跨仓协作。
- 架构调整、API/DB/权限/进程边界变化。
- 用户明确要求“做计划”“拆阶段”“推进 current”。

## 不适用场景

- L0 问答。
- L1 文案、样式、单文件小改。
- 没有范围变化的 bugfix。
- 已有阶段计划且本次只补一条验收证据。

## 最小上下文

1. `AGENTS.md`。
2. `docs/plans/current.md`，如果存在。
3. current 指向的 phase 文件。
4. 只有范围、架构、阶段变化时才读 project-plan。

## 工作流

1. 先判断是否真的需要计划。
2. 如果已有 `current.md`，先恢复当前阶段，不重建计划。
3. 把任务分成一个当前可执行阶段，不一次规划过远细节。
4. 阶段编号必须全局唯一，例如 `FE-01`、`BE-01`、`DB-01`、`OPS-01`。
5. 每个阶段写清目标、范围、不做事项、验收证据、回滚方式。
6. 只有范围或架构变化才更新 project-plan。
7. 阶段完成后再推进 current，不能凭感觉推进。

## 阶段模板

```md
# BE-01 API 基线

- 状态：in-progress
- 更新时间：YYYY-MM-DD

## 目标
- 建立后端 API 基线。

## 范围
- health / ready
- 错误响应 envelope
- API smoke

## 不做
- 不接生产数据库
- 不接真实外部 AI 服务

## 验收证据
- [ ] health smoke 通过
- [ ] 单测通过
- [ ] 契约已更新

## 回滚
- 移除本阶段新增 API 和配置。
```

## 常见合理化借口

| 借口 | 实际处理 |
|---|---|
| 小改也顺手补计划 | 不补，避免文档噪音 |
| current 不存在，直接重建全套计划 | 先建最小 current，不虚构长期路线 |
| 阶段完成了但没跑验证 | 不推进阶段 |
| 计划越细越安全 | 计划只细到下一步可执行 |

## 红旗

- 没读 current 就改 project-plan。
- 小任务创建一堆 phase 文件。
- 阶段没有验收证据。
- project-plan 里写了未经确认的事实。
- 阶段编号重复或只用 `01/02/03`。

## 验证清单

- [ ] 当前任务确实需要计划。
- [ ] 已恢复或创建 current。
- [ ] 阶段编号唯一。
- [ ] 阶段有验收证据。
- [ ] 未执行验证没有写成通过。
- [ ] project-plan 只在范围/架构/阶段变化时更新。

## 输出要求

交付时说明：当前阶段、是否更新计划、更新原因、下一步、阻塞项、验收证据和未验证项。
## 停止条件

- 项目目标、仓库边界、交付范围或验收标准不清。
- 需要决定单仓/多仓、技术栈、数据库或部署形态但用户未确认。
- 旧项目存在未提交改动或规则冲突，继续接入可能覆盖现有约定。
- 用户还没有确认计划书，不进入实际改造。
## 强制执行规则

- 一旦本 skill 被任务路由命中，必须按本文件工作流执行；不得只把它当作参考建议。
- 必须先读取本 skill 的最小上下文，再改文件、跑命令或给结论。
- 必须遵守项目级红线：不得写入或修改用户全局 Claude / Codex 配置、全局 rules、全局 skills、全局 hooks、全局 MCP。
- 必须优先使用项目 `.ai-spec/skills/`；读取用户本地 skill 前，必须说明项目 skill 缺口和本地 skill 名称。
- 遇到停止条件必须停下来报告，不得绕过、弱化或自行假设用户已确认。
- 涉及真实外部服务、生产数据、权限、安全、DB、文件删除、Git push、全局配置时，必须先取得用户确认。
- 最终输出必须说明：使用了哪个 skill、改了什么、验证了什么、未验证什么、是否触发停止条件。

