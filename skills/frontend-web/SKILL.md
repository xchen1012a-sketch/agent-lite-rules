---
name: frontend-web
description: 构建生产质量 Web 前端。用于 Vue、React、Vite、Next、Tailwind、组件、页面、状态管理、API client、响应式和可访问性工作。
---

# Frontend Web

## 目标

做真实可用的用户界面，而不是空壳页面或模板感 UI。页面要能完成目标流程，状态完整，响应式稳定，可访问，视觉遵守项目已有设计系统。

## 适用场景

- 新建或修改页面、组件、布局。
- 接 API、状态管理、表单、列表、筛选、上传、画布或工作台。
- 修复交互、视觉、响应式、可访问性问题。

## 不适用场景

- 纯后端 API。
- 纯数据库迁移。
- 不涉及用户界面的脚本或配置任务。

## 最小上下文

1. 目标页面或组件。
2. 相邻组件、样式、主题 token。
3. API client、mock 或 contract。
4. 相关测试和现有启动/构建命令。

## 工作流

1. 明确用户要完成的真实流程，不先做空 landing。
2. 查已有设计系统、组件库、图标库、颜色和间距规则。
3. 分离数据容器和展示组件；API 调用集中到 client/service。
4. 为 loading、error、empty、success、disabled 状态写 UI。
5. 移动端优先，至少考虑 320、768、1024、1440 宽度。
6. 文字不能溢出，按钮和控件不能互相遮挡。
7. 交互元素必须可键盘访问，有可理解 label。
8. 不使用泛滥的紫色渐变、大圆角卡片、堆卡片、假 hero 等 AI 默认审美，除非项目设计系统就是这样。
9. 完成后跑最小相关验证；涉及浏览器行为时做截图或 Playwright/DevTools 检查。

## 组件边界

- 页面负责布局和数据入口。
- 容器负责请求、加载、错误和状态转换。
- 展示组件只负责渲染和用户动作回调。
- 表单校验靠 schema 或统一规则，不散落在模板里。
- URL state 用于筛选、分页、可分享状态；不要滥用全局 store。

## 常见合理化借口

| 借口 | 实际处理 |
|---|---|
| 原型阶段不用管响应式 | 原型也会变成正式代码，先保证关键断点 |
| loading/error 以后补 | 这是基本用户体验，当前就补 |
| 先把接口字段猜出来 | 读 contract 或后端类型，不猜字段 |
| 紫色渐变看起来像 AI 产品 | 使用项目真实设计系统 |
| 截图不用看，代码能跑就行 | UI 任务必须检查实际渲染 |

## 红旗

- 页面只有静态卡片，没有真实流程。
- 组件超过 200 行还混着请求、业务和展示。
- 只有 happy path，没有错误和空状态。
- 移动端文字溢出或控件重叠。
- 原始 hex、任意 px、重复样式到处散落。
- 表格、弹窗、按钮不可键盘访问。

## 验证清单

- [ ] typecheck 通过或说明无法执行。
- [ ] build 通过或说明无法执行。
- [ ] 单测/组件测试覆盖关键逻辑。
- [ ] loading/error/empty 状态可见。
- [ ] 关键断点无溢出。
- [ ] 浏览器无明显 console error。
- [ ] API 字段来自契约或已确认来源。

## 输出要求

说明修改页面/组件、用户流程、状态覆盖、验证命令、截图/浏览器检查结果、未验证项和剩余风险。
## 停止条件

- 需要改变全局设计系统、路由结构或状态模型但用户未确认。
- API 字段、权限或错误形状不清，继续会靠猜契约。
- 需要接入真实支付、登录、上传、媒体或外部服务。
- 响应式布局、核心交互或可访问性无法完成最小验证。
## 强制执行规则

- 一旦本 skill 被任务路由命中，必须按本文件工作流执行；不得只把它当作参考建议。
- 必须先读取本 skill 的最小上下文，再改文件、跑命令或给结论。
- 必须遵守项目级红线：不得写入或修改用户全局 Claude / Codex 配置、全局 rules、全局 skills、全局 hooks、全局 MCP。
- 必须优先使用项目 `.ai-spec/skills/`；读取用户本地 skill 前，必须说明项目 skill 缺口和本地 skill 名称。
- 遇到停止条件必须停下来报告，不得绕过、弱化或自行假设用户已确认。
- 涉及真实外部服务、生产数据、权限、安全、DB、文件删除、Git push、全局配置时，必须先取得用户确认。
- 最终输出必须说明：使用了哪个 skill、改了什么、验证了什么、未验证什么、是否触发停止条件。

